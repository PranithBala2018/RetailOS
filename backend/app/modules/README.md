# Modules

Business modules (auth, company, products, inventory, purchases, sales_pos, etc.)
live here, one package per module, as defined in `SPRINT0.md` §2.2.

This directory is intentionally empty at the end of Sprint 1 — Sprint 1 scope is
infrastructure only. The first module (`auth` domain objects backed by real
persistence, `company`) lands in Sprint 2.
